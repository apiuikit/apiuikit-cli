---
"@apiuikit/cli": minor
---

`generate` and `validate` now accept a `http://`/`https://` URL for `<input>`, and `--config` accepts a URL too — the spec/config is fetched directly instead of requiring a local download first.
